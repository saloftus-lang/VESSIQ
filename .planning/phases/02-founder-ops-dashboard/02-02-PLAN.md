---
phase: 02-founder-ops-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - founder.html
autonomous: false
requirements:
  - FND-05
  - FND-06

must_haves:
  truths:
    - "Scrolling past the tasks/notes section reveals 'Industry Benchmarks' heading and 3 benchmark cards"
    - "Each benchmark card shows Industry avg value, VESSIQ target value, a progress bar with amber marker, and a green delta badge"
    - "Invoice Error Rate card shows Industry 38% vs VESSIQ <5% with delta '33pp better'"
    - "Avg Detention Days card shows Industry 4.2 days vs VESSIQ <1 day with delta '3.2 days less'"
    - "Dispute Win Rate card shows Industry 32% vs VESSIQ 68% with delta '+36pp advantage'"
    - "The entire page (header, KPIs, tasks, notes, benchmarks) renders without console errors at 1280x800 and 1440x900"
  artifacts:
    - path: "founder.html"
      provides: "Complete founder dashboard with all sections including benchmarks"
      contains: "Industry Benchmarks"
  key_links:
    - from: "benchmark card progress bars"
      to: "CSS width percentages"
      via: "inline style width on .bench-bar-fill elements"
      pattern: "bench-bar-fill"
    - from: "benchmark delta badges"
      to: "benchmark card data"
      via: "static HTML content matching D-17 values"
      pattern: "33pp better|3\\.2 days less|\\+36pp advantage"
---

<objective>
Add the Industry Benchmarks section to `founder.html` (3 benchmark cards with progress bars and delta badges) and verify the complete page works end-to-end.

Purpose: This completes the founder dashboard with the industry comparison data that helps Sean contextualize VESSIQ's performance against the market. After this plan, the entire Phase 2 deliverable is complete.

Output: `founder.html` fully complete with all 7 FND requirements satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-founder-ops-dashboard/02-CONTEXT.md
@.planning/phases/02-founder-ops-dashboard/02-RESEARCH.md
@.planning/phases/02-founder-ops-dashboard/02-UI-SPEC.md
@.planning/phases/02-founder-ops-dashboard/02-01-SUMMARY.md

<interfaces>
<!-- Benchmark card specs from UI-SPEC and RESEARCH -->

From 02-UI-SPEC.md Benchmark Cards Row section:

Card structure (top to bottom per card):
- Metric Name: 14px, weight 700, #FFFFFF
- Description: 12px, #94A3B8, margin-bottom 16px
- Industry avg row: label 11px uppercase #94A3B8 + value 14px weight 700 #E2E8F0 JetBrains Mono
- Progress track with industry fill + VESSIQ amber marker
- VESSIQ target row: label 11px uppercase #94A3B8 + value 14px weight 700 #F59E0B JetBrains Mono
- Delta badge pill: green bg rgba(16,185,129,0.15), text #10B981

From 02-RESEARCH.md Pattern 5 CSS:
```css
.bench-bar-track {
  height: 6px;
  background: rgba(255,255,255,0.08);
  border-radius: 3px;
  overflow: hidden;
  margin-top: 4px;
  position: relative;
}
.bench-bar-fill {
  height: 100%;
  border-radius: 3px;
}
.bench-bar-fill.industry { background: rgba(255,255,255,0.25); }
```

VESSIQ amber marker overlay on track:
```css
.bench-marker {
  position: absolute;
  top: -3px;
  width: 2px;
  height: 12px;
  background: #F59E0B;
  border-radius: 1px;
}
```

Benchmark data (from CONTEXT.md D-17, UI-SPEC):
1. Invoice Error Rate: Industry 38% (bar 38%), VESSIQ <5% (marker 5%), delta "33pp better"
2. Avg Detention Days: Industry 4.2 days (bar 84% of 5-day max), VESSIQ <1 day (marker 20%), delta "3.2 days less"
3. Dispute Win Rate: Industry 32% (bar 32%), VESSIQ 68% (marker 68%), delta "+36pp advantage"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark cards section and CSS to founder.html</name>
  <files>founder.html</files>

  <read_first>
    - founder.html (current state from Plan 01 — understand where to insert benchmark section)
    - .planning/phases/02-founder-ops-dashboard/02-UI-SPEC.md (Benchmark Cards Row section and Copywriting Contract)
    - .planning/phases/02-founder-ops-dashboard/02-CONTEXT.md (D-17, D-18 benchmark decisions)
    - .planning/phases/02-founder-ops-dashboard/02-RESEARCH.md (Pattern 5: Benchmark Card with Progress Bar)
  </read_first>

  <action>
Modify `founder.html` to add the Industry Benchmarks section. Insert both new CSS rules and new HTML.

**CSS additions — add these rules to the existing `<style>` block:**

Section heading for benchmarks (reuses `.section-heading` from Plan 01, just needs margin-top override):
```css
.section-heading.benchmarks { margin-top: 48px; }
```

Three-column layout for benchmark cards:
```css
.three-col {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
  margin-bottom: 48px;
}
@media (max-width: 900px) {
  .three-col { grid-template-columns: 1fr; }
}
```

Benchmark card internals:
```css
.bench-card .bench-metric {
  font-size: 14px;
  font-weight: 700;
  color: #FFFFFF;
  margin-bottom: 4px;
}
.bench-card .bench-desc {
  font-size: 12px;
  color: #94A3B8;
  margin-bottom: 16px;
  line-height: 1.4;
}
.bench-row {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-bottom: 4px;
}
.bench-label {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: #94A3B8;
}
.bench-value {
  font-family: 'JetBrains Mono', monospace;
  font-size: 14px;
  font-weight: 700;
  color: #E2E8F0;
}
.bench-value.target {
  color: #F59E0B;
}
.bench-bar-track {
  height: 6px;
  background: rgba(255,255,255,0.08);
  border-radius: 3px;
  position: relative;
  margin: 8px 0 16px;
}
.bench-bar-fill {
  height: 100%;
  border-radius: 3px;
  background: rgba(255,255,255,0.25);
}
.bench-marker {
  position: absolute;
  top: -3px;
  width: 2px;
  height: 12px;
  background: #F59E0B;
  border-radius: 1px;
}
.bench-delta {
  display: inline-block;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 11px;
  font-weight: 700;
  background: rgba(16,185,129,0.15);
  color: #10B981;
  margin-top: 8px;
}
```

**HTML additions — insert AFTER the closing `</div>` of the `.two-col` container (tasks + notes) and BEFORE the closing `</div>` of `.content`:**

```html
<h2 class="section-heading benchmarks">Industry Benchmarks</h2>

<div class="three-col">
  <!-- Card 1: Invoice Error Rate -->
  <div class="glass-card bench-card">
    <div class="bench-metric">Invoice Error Rate</div>
    <div class="bench-desc">Percentage of invoices containing billing errors</div>
    <div class="bench-row">
      <span class="bench-label">Industry avg</span>
      <span class="bench-value">38%</span>
    </div>
    <div class="bench-bar-track">
      <div class="bench-bar-fill" style="width: 38%"></div>
      <div class="bench-marker" style="left: 5%"></div>
    </div>
    <div class="bench-row">
      <span class="bench-label">VESSIQ target</span>
      <span class="bench-value target">&lt;5%</span>
    </div>
    <div class="bench-delta">33pp better</div>
  </div>

  <!-- Card 2: Avg Detention Days -->
  <div class="glass-card bench-card">
    <div class="bench-metric">Avg Detention Days</div>
    <div class="bench-desc">Average days cargo held beyond free time before dispute resolution</div>
    <div class="bench-row">
      <span class="bench-label">Industry avg</span>
      <span class="bench-value">4.2 days</span>
    </div>
    <div class="bench-bar-track">
      <div class="bench-bar-fill" style="width: 84%"></div>
      <div class="bench-marker" style="left: 20%"></div>
    </div>
    <div class="bench-row">
      <span class="bench-label">VESSIQ target</span>
      <span class="bench-value target">&lt;1 day</span>
    </div>
    <div class="bench-delta">3.2 days less</div>
  </div>

  <!-- Card 3: Dispute Win Rate -->
  <div class="glass-card bench-card">
    <div class="bench-metric">Dispute Win Rate</div>
    <div class="bench-desc">Percentage of disputed invoices resolved in client's favor</div>
    <div class="bench-row">
      <span class="bench-label">Industry avg</span>
      <span class="bench-value">32%</span>
    </div>
    <div class="bench-bar-track">
      <div class="bench-bar-fill" style="width: 32%"></div>
      <div class="bench-marker" style="left: 68%"></div>
    </div>
    <div class="bench-row">
      <span class="bench-label">VESSIQ target</span>
      <span class="bench-value target">68%</span>
    </div>
    <div class="bench-delta">+36pp advantage</div>
  </div>
</div>
```

**Key implementation details:**
- Benchmark progress bar widths: Card 1 fill = 38%, marker at 5%. Card 2 fill = 84% (4.2/5 scale), marker at 20% (1/5 scale). Card 3 fill = 32%, marker at 68%.
- The `<5%` and `<1 day` values use `&lt;` HTML entity (not raw `<`) to avoid HTML parsing issues.
- No JavaScript needed for benchmarks — they are static display-only cards (per UI-SPEC: "No hover animation on benchmark cards").
- Do NOT add any hover effects to benchmark cards (they are display-only, not interactive).
  </action>

  <verify>
    <automated>cd /Users/seanloftus/Desktop/VESSIQ && grep -q "Industry Benchmarks" founder.html && grep -q "bench-bar-track" founder.html && grep -q "33pp better" founder.html && grep -q "3.2 days less" founder.html && grep -q "+36pp advantage" founder.html && grep -q "bench-marker" founder.html && grep -c "glass-card bench-card" founder.html | grep -q "3" && echo "PASS"</automated>
  </verify>

  <acceptance_criteria>
    - founder.html contains `<h2 class="section-heading benchmarks">Industry Benchmarks</h2>`
    - founder.html contains exactly 3 elements with class `glass-card bench-card`
    - Card 1 contains: metric name "Invoice Error Rate", description "Percentage of invoices containing billing errors", industry value "38%", VESSIQ value "&lt;5%" (or `<5%`), delta "33pp better"
    - Card 2 contains: metric name "Avg Detention Days", description including "free time", industry value "4.2 days", VESSIQ value "&lt;1 day" (or `<1 day`), delta "3.2 days less"
    - Card 3 contains: metric name "Dispute Win Rate", description including "client's favor", industry value "32%", VESSIQ value "68%", delta "+36pp advantage"
    - Card 1 progress bar: fill width 38%, marker at left 5%
    - Card 2 progress bar: fill width 84%, marker at left 20%
    - Card 3 progress bar: fill width 32%, marker at left 68%
    - `.bench-bar-track` CSS has `height: 6px` and `position: relative`
    - `.bench-marker` CSS has `position: absolute`, `background: #F59E0B`, `height: 12px`
    - `.bench-delta` CSS has `background: rgba(16,185,129,0.15)` and `color: #10B981`
    - `.bench-value.target` CSS has `color: #F59E0B`
    - `.three-col` CSS has `grid-template-columns: repeat(3, 1fr)` and `gap: 24px`
    - Benchmark section appears AFTER the two-col tasks/notes section in the HTML
    - No JavaScript added for benchmarks (static HTML only)
  </acceptance_criteria>

  <done>
    Scrolling down past tasks/notes reveals "Industry Benchmarks" heading and 3 glassmorphism cards, each showing: metric name, description, industry avg with value, a 6px progress track with a white fill bar and an amber vertical VESSIQ marker, VESSIQ target in amber text, and a green delta badge. All values match D-17 exactly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete founder dashboard</name>
  <files>founder.html</files>
  <action>
    Human visual verification checkpoint. No code changes — this task pauses execution for the user to confirm the complete founder dashboard looks and works correctly across all 7 FND requirements. The executor should start a local HTTP server (`python3 -m http.server 8080` from project root) if not already running, then present the verification checklist to the user.
  </action>
  <what-built>
    Complete founder.html with all 7 FND requirements:
    - FND-01: Header with VESSIQ logo and amber "Founder View" badge
    - FND-02: 3 KPI tiles (Active Pilots = 1, Invoices Processed = 23, Total Savings = $187,500) with GSAP count-up
    - FND-03: Ops tasks widget with add/complete/delete and localStorage persistence
    - FND-04: Notes widget with auto-save to localStorage
    - FND-05: 3 industry benchmark cards (Error Rate, Detention Days, Dispute Win Rate)
    - FND-06: Progress bars with amber VESSIQ marker + green delta badges on each benchmark
    - FND-07: Dark navy glassmorphism design system shared with dashboard.html
  </what-built>
  <how-to-verify>
    1. Start server: `cd /Users/seanloftus/Desktop/VESSIQ && python3 -m http.server 8080`
    2. Open http://localhost:8080/founder.html in Chrome
    3. Verify header: VESSIQ logo on left, amber "Founder View" badge on right
    4. Verify KPI tiles: 3 cards count up on load — check final values are 1, 23, $187,500
    5. Scroll to "This Week" — verify tasks and notes are side by side
    6. Click a task checkbox — text should strikethrough, refresh page — state should persist
    7. Type in notes textarea — refresh page — text should persist
    8. Add a new task via input + Add button — verify it appears
    9. Delete a task via x button — verify it disappears
    10. Scroll to "Industry Benchmarks" — verify 3 cards with progress bars and delta badges
    11. Check overall feel: dark navy glassmorphism, no broken layout, no console errors
    12. Resize window to 1280x800 — verify nothing breaks
  </how-to-verify>
  <verify>User confirms "approved" or provides feedback for iteration</verify>
  <done>User has visually confirmed the complete founder dashboard meets all FND requirements and looks correct</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `founder.html` contains all 7 FND requirement implementations
2. All localStorage persistence works across page refresh
3. No console errors at 1280x800 and 1440x900
4. Visual design matches dark navy glassmorphism spec
5. KPI values: 1, 23, $187,500 (matching dashboard.html DATA consistency)
6. Benchmark values: 38%/<5%, 4.2 days/<1 day, 32%/68% with correct delta badges
</verification>

<success_criteria>
- founder.html is a complete standalone HTML file implementing all FND-01 through FND-07 requirements
- Human verification confirms visual quality and interactive functionality
- Page renders without console errors at 1280x800 and 1440x900
- All localStorage persistence (tasks + notes) works across page refresh
- Design system (colors, fonts, glassmorphism) is visually consistent with the dark navy spec
</success_criteria>

<output>
After completion, create `.planning/phases/02-founder-ops-dashboard/02-02-SUMMARY.md`
</output>
